# H-04 操作手册 —— 网络矩阵实测

> **你来做，我做不了。** 拿手机+电脑跑场景，每个场景连 20 次。
> **正式数据台账在 [h04-network-matrix.md](h04-network-matrix.md)**（含判定标准与原始日志指引），
> 本文只管"怎么跑"和各场景的当前状态。

---

## 8 个场景：怎么搞 + 当前进度（2026-07-29）

| # | 场景 | 手机连什么网 | 电脑连什么网 | 怎么搞 | 状态 / 结论 |
|---|------|------------|------------|--------|------------|
| 1 | **同 WiFi** | 家里 WiFi | 家里 WiFi | 最简单——手机和电脑（Mac mini **宿主机**，别在 VM 里）都在家连同一个 WiFi | ⏳ **待跑**（优先级最高，到家就跑） |
| 2 | **5G → 家宽** | 关 WiFi，用 4G/5G | 家里 WiFi | 手机切蜂窝，电脑在家不动 | 🟡 **已跑，判定暂缓**：0/20 direct，relay 兜底 20/20 完成 11.3 Mbps。按**复测矩阵 R1~R4 + 阿里云对照 C1**（见 h04-network-matrix.md 归因修正节）重跑，R1（宿主机+关代理）优先 |
| 3 | **4G → 家宽（反向）** | 家里 WiFi | 手机开热点给电脑连 | 手机 A 开热点→电脑连热点当"蜂窝侧"；另一台手机连家里 WiFi 拨入。反过来（电脑开热点）也行，核心是一边蜂窝一边宽带、由宽带侧发起 | ⏳ 待跑。注意：App 当监听端有缺陷④（ticket 不等 relay），**必须用 v2** |
| 4 | **双运营商 4G** | 4G SIM A | 另一张卡开热点 | 两张不同运营商 SIM，一张手机直连 4G，另一张开热点给电脑连。**仅当有两张不同运营商 SIM 时做** | ⏸️ 机会主义（目前只有一张 SIM） |
| 5 | **同运营商 4G** | 4G SIM A | 同卡开热点 | 同一张 SIM，手机连 4G，电脑连该手机热点。**仅当有多张同运营商 SIM 时做，否则并入 #4** | ⏸️ 机会主义（同上） |
| 6 | **公共 WiFi** | 咖啡店/酒店/商场 WiFi | 家里 WiFi | 拿手机出去找个公共 WiFi（咖啡店最好），电脑在家不动。**出门那天顺便做** | ⏳ 出门顺便 |
| 7 | **公司/企业网** | 公司 WiFi | 家里 WiFi | 上班时手机连公司 WiFi 跑，电脑在家不动。**工作日做** | ✅ **完成**：20/20 direct，P50 23ms / 16.9 Mbps，0 失败。*2026-07-29 修正：实际是 v4 打洞穿双层 NAT（"全 IPv6"系 App 分类器缺陷）* |
| 8 | **IPv6-only** | 5G IPv6 | WiFi IPv6 | 需确认两边都有 IPv6 地址（手机拨号 `*#*#4636#*#*` 或设置里看；Mac `ifconfig \| grep inet6` 有 `240x:` 开头的全局地址即是）。**监听端放宿主机**（VM 无 v6） | ⏳ **重新打开**：原"等效完成"依据的 v6 数据因分类器缺陷作废；需 APK v3 + 宿主机监听端重测 |

数据明细、判定依据、复测清单 → [h04-network-matrix.md](h04-network-matrix.md)；原始 JSONL → [h04-logs/](h04-logs/)。

**对照实验（2026-07-29 已完成）**：✅ C1 鸿蒙 5G→阿里云公网 IP **20/20 direct/v4**（蜂窝侧无限制，
场景 2 归因收敛到家侧）；✅ C2 三星公司 WiFi→阿里云 **20/20 direct/v4** P50 24ms/16.9Mbps（分流代理
不影响对国内公网 v4 的直连）。数据见 h04-network-matrix.md C1/C2 节。
剩余计划：阿里云自建 relay 做国内/SG relay 质量 A/B（H-07 试点）。

---

## 准备工作（一次性）

### 1. 安装 Android Probe App —— **必须用 v3**

v3 APK 位置：`~/Downloads/p-pass-probe-debug-v3.apk`（2026-07-29 构建）。

- **三星 S24**：插线 `adb install -r ~/Downloads/p-pass-probe-debug-v3.apk`
- **鸿蒙**：把 APK 传到手机自装（经卓易通）

⚠️ v1 有四个缺陷（错误信息恒空、Share Log 按钮会消失、切后台丢状态、监听 ticket 不等 relay）；
v2 修了这四个但 **ipver 字段仍不可信**（分类器缺陷，所有 direct 都记成 v6）。
v3 修复分类并新增 `remote` 字段。**只认 v3。**

### 2. 电脑端 iroh-probe CLI

```bash
cd ~/github/P-Pass/spikes/iroh-probe   # 家里 Mac mini 是 agent 的 workspace 路径
cargo run --release -- listen
```

保持终端开着，屏幕会打出 **ticket**（`endpoint…` 开头的一长串）。

**ticket 是固定的，抄一次就行**：密钥持久化在 `iroh-probe.key`、端口钉死 41145，
重启进程 ticket 不变。CLI 会先等 relay 就绪（最多 15s）再打印 ticket，等它打出来再复制。

---

## 测试操作（每场景 5~8 分钟）

1. **电脑**：确认 iroh-probe 还在 listen（重启也没关系，ticket 不变）
2. **手机**：Probe App → 粘贴 ticket → **Bind** → **Start UIDT Stress Test**（20 次 ×100MB）
3. **等**：每轮显示 path（direct/relay）、延迟、速度；**中途别点其他页签**（v2 状态已进程级保持，但少折腾）
4. **导出**：点 **Share Log**（v2 常驻，任何时候都在）→ 把 JSONL 发我
5. 我来汇总回填台账

---

## 顺便观察（边跑边记，不强制）

- 📱 **锁屏影响**：已有一条记录（场景 7 第 16 轮灭屏不断传，短锁屏不触发 Doze）；长锁屏（>15 分钟）还没测
- 🔄 **网络切换**：WiFi ↔ 蜂窝切换后连接能否恢复
- ⏰ **NAT 保活**：连上后放 30 分钟，还能不能继续传

---

## FAQ

**Q: ticket 在哪个终端？**
A: 跑 `listen` 的那个。它固定不变，存备忘录里即可。

**Q: 跑失败了/打不开？**
A: 截图/把 Share Log 的 JSONL 直接扔给我，我来排查。

**Q: 要一次性全跑完？**
A: 不用。剩下的核心就两个：**场景 1（到家 30 分钟）** 和 **场景 2 复测（宿主机+关代理）**。
其余出门/借到 SIM 顺便做。跑几个发几个日志。
