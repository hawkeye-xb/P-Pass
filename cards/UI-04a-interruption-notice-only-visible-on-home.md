# UI-04a 中断提示只在总览页可见，切到别的 tab 就看不到　级别 L2

> 🟠 状态：已认领，等待 CI-03/I18N-02 批次合入 main 后开发（2026-09-08）
> 当前节点：复用 `HomeNotices.kt` 的全局呈现骨架；下一步：把中断提示迁出 `HomeScreen` 的局部布局。
> 协同分支：`batch/ui-04a-c`
> 级别：L2 · 阻塞：本批 main 集成

## 问题

2026-08-26 真机走查，验收人反馈：

> 「客户端常驻通知为什么只出现在总览？不合理吧？」

中断提示（`MOB-28`）渲染在 `HomeScreen` 里。但它表达的是**全局状态**（后台
备份停了），用户在照片页/设置页一样需要知道。切到别的 tab 就看不见了。

## 期望行为

全局状态类提示（中断、重传告知、白名单建议）在**所有页面**可见，或至少在
tab 上有角标能把人引回去。

## 验收标准

- [ ] 单测/走查：中断提示在照片页、设置页同样可见（或 tab 有角标）

## 范围

- 只准动：`apps/android/.../ui/`（提示的呈现与优先级）、`res/values*/strings.xml`
- 不准动：各提示**触发条件**的判定逻辑（`MOB-28` 的开机时刻判据、
  `DOG-02b` 的五条件等）——本卡只管「怎么显示」，不动「什么时候该显示」

## 阻塞与依赖

无。⚠️ 与 UI-04b/UI-04c 共享同一处提示呈现层——`MOB-37` 已经把提示骨架搭好
（`ui/HomeNotices.kt`），三张卡里**谁先落地谁把优先级框架搭好**，后面的接
进去，别各搭一套。
