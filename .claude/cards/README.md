# 任务卡队列（执行 agent 工作入口）

- 本目录 = 当前待做的任务卡，一卡一文件，格式按 `docs/AGENT_PROTOCOL.md` §C.2。
- 领卡即开工，做完把卡移到 `done/` 并在卡尾追加验收记录（证据/测试输出摘录）。
- 优先级看卡内标注；无标注按文件名字典序。
- 流程纪律（直推 main 的三条底线、tag 纪律）见仓库根 `CLAUDE.md`——先读它。
