# tools/gen-kotlin

proto JSON Schema → Kotlin data class generator (T-050).
CI drift check: regeneration must leave git diff empty.

---

proto JSON Schema → Kotlin data class 生成器（T-050）。
CI 漂移检查：重新生成后 git diff 必空。
