# P-Pass 自建环境变量（T-063 / T-063b）。cp .env.example .env 后编辑。
# 域名需先解析 A 记录到 VPS IP（详见 SELFHOST.md）。

# rendezvous 服务的公网域名（caddy 自动申请证书；只部署 relay 时可忽略）
RENDEZVOUS_DOMAIN=rendezvous.example.com

# relay 域名（relay-config.toml 的 tls.hostname 里也要写同一个；
# cloud-init 模板用 sed 替换这里和 relay-config.toml 两处）
# 客户端 relay_urls 写法：https://<此域名>:8443
RELAY_DOMAIN=relay.example.com
