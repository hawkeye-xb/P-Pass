# UI-04d 重传通知挂在「照片备份失败」渠道下，分类名不对　级别 L2

> ⬜ 状态：未开工——与 MOB-69 同根因，重新定级（2026-09-14 复核）
> 当前节点：`noteReuploadNotice()`（生产发送重传系统通知的函数）在生产代码里**零调用点**，`ReuploadQueue.kt` 头部标注"LEGACY BATCH STATE — frozen by REBUILD-00"。当前重传告知只走 App 内 `HomeNotices.kt` 卡片，不发系统通知，所以"渠道名不对"这个现象目前不存在（没有通知可分类）。真正待决问题是 MOB-69：REBUILD-04 是否要把重传通知重新接回生产。
> 下一步：等 MOB-69 拍板"重传要不要有系统通知"。若要接回，直接用新渠道名实现（不存在"改名"这一步，是从零建）；若拍板不接回，本卡该关闭为"已被上游决策取代"。
> 协同分支：`main`
> 级别：L2 · 阻塞：MOB-69 的拍板结果

## 问题

`MOB-37` 实施时发现：重传告知的系统通知复用了 `FAIL_CHANNEL_ID`，而那个渠道
对用户显示的名字是**「照片备份失败 Backup failed」**。于是「正在把照片重新传
回来」这条**正常进展**的通知，出现在一个叫「失败」的分组里——用户在系统通知
设置里看到的分类是错的，想单独关掉某一类也做不到。

这是 `MOB-29` 的遗留（当时只有失败通知一个渠道）。

## 期望行为

重传告知有自己的通知渠道（名字表达「进展」而不是「失败」），用户能单独
关掉它而不影响失败通知。

## 验收标准

- [ ] 重传告知有自己的通知渠道，渠道名不含"失败"字样
- [ ] 用户可在系统通知设置里单独关闭重传告知渠道，不影响失败通知渠道

## 范围

- 只准动：`apps/android/.../ui/`（通知渠道定义）、`res/values*/strings.xml`
- 不准动：重传告知**触发条件**（`MOB-29`/`MOB-37` 的判定逻辑）

## 阻塞与依赖

无。
