> ❄️ **已冻结（2026-09-18）**：本模板已被 GitHub issue 模板
> `.github/ISSUE_TEMPLATE/task-card.md` 取代，新开工作请用 issue。本文件只读留档。

# 卡号 标题

状态：🟥 挂号 / ⬜ 可接 / 🟡 代码已合并待验收 / ✅ 已归档（commit xxx）
级别：L0-L3（拿不准往高放；L0 机械 / L1 局部 / L2 跨模块 / L3 真环境）
requires:（可接卡才填：需补读的文档，无则删行）
关联:（从 X 分出 / 阻塞 Y / 被 Z 阻塞；关系变化时双向同步，不靠对话记忆）

## 挂号段（发现时 30 秒填完；🟥 状态只许有这三行，不可被接）

- 现象：
- 发现场景：（在做哪张卡/什么操作时撞见）
- 严重度猜测：

## 可接段（🟥→⬜ 由派活时补满，缺字段=不可接）

context:（根因定位、文件:行号、当前测试基线、已知坑——让 agent 不考古就能开工）
问题:（现象 + 复现步骤，不假设读者看过别的卡）
期望行为:
验收标准:（可逐条打勾；每条标证据等级 [E1-E4]，E4 不单独作数；
  故障/安全类必带反证：去掉故障条件必须变红）
范围:（只准动这些文件 + 明确"不准动"的相邻物）
阻塞与依赖:（等拍板的决定 / 前置卡 / 需要的环境）

## 实施记录（做完填）

验收证据：命令 + 真实输出，注明各条证据等级
实施中追加：（同范围、顺手做掉的，逐条列出）
