# DESK-04 桌面向导低成本对齐　级别 L1

## 语义基准

decisions ⑥：T-042 三步向导流程保留，**不重做**。

## 修法

1. 文案按产品语言过一遍（先说人话、不出现技术词；「照片存到哪」而非
   「选择库目录」口径，参考 layout-v1 文案密度）。
2. 视觉 token 化（--pp-* / tokens.json，不许残留硬编码色）。
3. 第三步二维码接 T4 之后的新配对弹窗流（扫码后即时切确认列表，
   IPC-02 事件驱动），别再是旧的常驻 QR。

## 可执行验收

1. 向导三步逐屏截图对照（文案/配色/QR 行为）；grep 无硬编码旧色值。
2. 走完向导 → 配对 → 确认列表即时出现（事件驱动生效）。
3. vite build 绿。

## 收尾
CI 绿；文档三件套；卡移 done/。

---

## 验收记录（2026-08-12 Salamira）

**实现**（低成本对齐，T-042 流程不重做）：
1. **文案按产品语言过一遍**：步骤名「选择照片位置/电源检查/添加手机」
   →「照片存在哪/电脑会睡吗/加手机」；h1「照片存到哪里？」→「照片存在
   哪里？」；去技术词——「访达」→「电脑自带的文件管理器」、「整体拷走」
   →「整个拷走」、「后台服务注册为系统常驻服务」→「开机自动运行、意外
   退出自动恢复——之后你不需要手动打开它」；「设为常驻服务并继续」→
   「继续」。
2. **视觉 token 化**：全组件已 var(--pp-*)，本次无新增硬编码（grep
   复核零残留）。
3. **第三步接 T4 新配对流**：Wizard 监听 `daemon-event`（IPC-02 事件
   驱动）+ 3s pending 轮询兜底；有人扫码（pairing.pending 非空）→
   即时切确认列表（逐行允许/拒绝，处理完该行消失），不再停留常驻 QR；
   confirmPair 复用 pairing.confirm 语义（device_name 精确确认）。

**验证**：
- `npm run build`（vite）绿，176 modules transformed，dist 产物正常。

**真机待验（C 线挂账）**：
1. 向导三步逐屏截图对照（文案/配色/QR 行为）。
2. 走完向导 → 配对 → 确认列表即时出现（事件驱动生效，真机扫码）。
