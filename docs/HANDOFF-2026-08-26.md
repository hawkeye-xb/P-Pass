# 交接件 · 2026-08-26

> 这一天信息量很大，写给**下一个 session 的自己**（人或 agent）。
> 目标是 30 秒内知道「现在在哪、下一步做什么、哪些坑别再踩」。
> 权威状态仍在 `docs/CHECKLIST.md`（待办）与 `docs/PROGRESS.md`（历史），
> 本文件只做快速启动。

## 30 秒版

- **阶段**：真机回归驱动的修 bug 循环。代码侧健康（本机全绿），**卡住的是真机验收**。
- **最新可测版本**：**`v0.4.0-test.9`**（已 tag + push，含 MOB-38 + UX-13 + **MOB-40** + **UX-14** + CI-04①）。
- **下一步**：① 验收人批签名门 → 装 test.9 → 跑一轮回归 ② 之后做 `MOB-39`（触发层重构）。
- **本机全绿基线**：`just ci` all green · nextest **320 passed / 1 skipped** ·
  Android **46 类 / 347 tests / 0 failures** · 桌面 `pnpm test 24` +
  `src-tauri cargo test --lib 15`。

## 环境事实（别重新摸索）

| 事项 | 状态 |
|---|---|
| 验收人的照片库 | `~/Pictures/P-Pass 家庭照片库`，**233 张真照片**。**一个字节都不许碰** |
| 手机 | 三星 SM-S9210（`RFCX1040SNE`）。**test.9 待装**。**不许做 adb 写操作** |
| 桌面 | `/Applications/P-Pass.app` = `0.4.0-test.5`，daemon 常驻服务已注册 |
| 手机日志 | 持续抓取中 → `/tmp/ppass-logs/logcat.txt`（三星 logcat 缓冲很小，不抓就丢） |
| daemon 日志 | `~/Library/Logs/p-pass-daemon.err` / `.log`（**别用 App 的「导出日志」**，见 DESK-10） |
| `gh` | **未登录**，仓库私有 → **看不到 GitHub Actions 结论**。每次 push 后的 CI 要验收人自己扫 |
| 备份范围 | 实测：**除 Lark 外全选**（Camera 在范围内）。库里 204 = 全机 216 − Lark 12 |

## 今天落地了什么（全部 🟡 等真机验收）

| 卡 | 一句话 | commit |
|---|---|---|
| `MOB-33` **L0** | 暂停改管线级（按 id 取消）+ 一次只跑一轮（`backupInFlight`）+ 进度条不乱跳 | `59ecab3` |
| `MOB-34` 补正 | 定向补偿两路并集（`files` ∪ `HashCache`）——**删掉了一条假的「已知边界」** | `480759f` |
| `MOB-35` | 中断待确认时前台该传就传 + 两处「顺带重挂」门控 | `8bbe178` |
| `MOB-36` | 移进已选相册的老照片进本轮候选（范围补齐） | `55f8c43` |
| `MOB-37` | 重传告知落盘，通知丢了 App 里还看得到 | `94574b1` |
| `MOB-38` **L0** | 回到前台就补捞（挂进 `ON_RESUME`，门控提成一份） | `f961cc5` |
| `DESK-10` 补漏 | 脱敏按「值的形状」做，`detail` 里的路径不再带全长 NodeId | `66a0038` |
| `UX-13` | 暂停后按钮不消失，原地变「继续」，走同一条管线 | `2315259` |

**真机已验通过的**：`MOB-34`（09:58:50 删 → 09:59:11 自动传回，21 秒）、
`MOB-35`（重开 App 前台自己备份）、`MOB-36`（受控实验：水位之下的照片移进已选相册，
App 在后台，1 秒被拾起）、`MOB-32`/`BLOB-01`/`WATCH-07`/`WATCH-03`/`WATCH-04`。

## 下一步：按这个顺序

1. ~~**UX-13**（L1）暂停后按钮变「继续」，别消失~~ — 🟡 代码已合并（`2315259`），等真机验收
2. **MOB-40（L0，当天真机打回后修完）**：还没选相册就把整库传了。`null`
   （从未选过范围）被全链路解释成「全量备份」。logcat 铁证：全新安装
   15:53 → 15:54:14 那一轮 `scanning 254/254`，用户此刻还没进选相册页；
   15:55:26 选完之后才是正确的 `offered=11`。闸门修在**管线咽喉**（worker
   读范围那一处，扫描之前），不逐个堵五条触发通道——MOB-33/34/35/38 四个
   bug 全是「漏接一处」。顺带修掉一条自钉字面语义的旧测试。
3. **UX-14（L1，test.8 回归当天修完）**：传一半自己「暂停」了。验收人没误触——
   失败走 `Result.retry()`，而 **WorkManager 的 retry 结构上拿不到
   `outputData`** → 那一轮不盖 `KEY_FINISHED_AT` → UX-13 的判据认为那次暂停
   「还没被覆盖」→ 又冒出「继续」。判据锚点从「最新完成时刻」扩到「最新
   **开跑**时刻」（新增 `RunStartPrefs`，worker 在抢到互斥门之后、扫描之前落盘）。
4. **test.8 真机回归 24 步：MOB-40 通过**（只选 11 张的相册 → 全程只传 11 张）。
   新开 `DOG-03` / `NET-01` / `I18N-01`，`UI-05` / `UI-06` / `DESK-11` 进 backlog。
5. **出 test.9**，验收人跑一轮回归
3. **MOB-39**（L1）触发层抽象重构 —— **架构方案见那张卡，图见 artifact**
4. 之后：`UI-04`（提示呈现四连）、`DESK-09`（向导透出 daemon 真错误）

`docs/CHECKLIST.md`：§一 **21 条**待真机验收，§三 **14 条**待做队列。

## 这一天最贵的五条教训

### 1. 守卫测试要钉「不变量」，别钉「字面形状」——今天误伤四次

四次都是同一个形状，然后正当的重构改了那串字面量，测试就红，而它**并没有多守住
任何东西**：

```
contains("if (backupInterrupted) return@LaunchedEffect")       ← MOB-35 那条
contains("enqueueReuploads(store.load(), reuploads, lost)")    ← MOB-34 那条
contains("return Result.success()")                            ← 我自己十分钟前写的
sliceBetween(…, "LaunchedEffect(backupInterrupted) {", …)      ← MOB-38 搬家后成批红
```

⚠️ **`MOB-39` 会搬动大量逻辑，这类源码文本断言会成批变红。届时要改的是切片位置，
不是不变量。** 这条已写进 `ForegroundSyncNotFrozenTest` 的注释里。

### 2. 别用正则去猜作用域

我加过一条「不许用 `!backupInterrupted` 把某调用包住」的正则断言，**当场误报**：
Kotlin 单语句 `if` 没有花括号，`if (cond) a()` 之后紧跟的 `b()` 其实在 `if` 之外，
而正则按「N 字符内出现」判命中。**文本匹配做不到作用域判定。**

### 3. 把「我没想到怎么做」写成「已知边界」，等于给错误结论盖章

`MOB-34` 第一版把「存量条目反查不到」写成已知边界、还配测试钉它，我还据此推荐了
「启动时一次性全量迁移」。验收人一句「这不是一个 json 吗？为什么会丢失？重建的话
hash 算出来不一致吗？」把它推翻了——**没有丢失，只是「hash → 本地 _ID」这一个方向
的索引没存；而那张表（`hash-cache.json`）一直在盘上，跨版本跨配对存活。**

### 4. 「天然一次性，连去重窗口都不用做」这种话要警惕

我把 `MOB-29` 通知的一次性说成优点。**那是把缺陷说成优雅**——一次性 = 没有重试 =
发失败即永久静默。省掉一套机制之前先问「省掉的那套本来在防什么」。→ `MOB-37`。

### 5. 验收人说「还是不 OK」时，先做受控实验分清两件事

「机制没做」vs「这次情形不同」。`MOB-36` 那次是后者（他从 Camera 移到 P-Pass，
**两个都是已选相册**，那张早就备份过了 → 正确行为就是什么都不发生）。**直接改代码
就白改了。**

## 派 subagent 的正确姿势（验收人明确批评过）

**别让 agent 先读一堆文档。** 之前的派活单开头是「先读 CLAUDE.md / AGENT_PROTOCOL.md
/ 卡 / TEMPLATE」——那是纯重复消耗（一个 agent 花 20 分钟重新挖我已知的东西）。

正确做法：**把事实内联进指令**——根因、文件、行号、原代码、现有类型定义、已知的坑、
测试计数基线、红线清单。唯一让它读的是那张卡本身。样例见
`MOB-38`/`UX-13` 的派活单（后者存于本轮 scratchpad）。

规则：**派「成本在做」的活，不派「成本在知道」的活。** 十行的改动 + 完整仪式，
仪式成本比改动大一个数量级——那种自己做。

## 几个反复咬人的工具坑

- **`./gradlew` 找不到 Java 时退出码是 0** → 假绿。必须
  `export JAVA_HOME=/opt/homebrew/opt/openjdk`，且**报绿必须给测试计数**
  （从 `test-results/*.xml` 数 + 核对时间戳）。
- **`git add -A` 会吞掉 `.claude/worktrees/`** → 已加进 `.gitignore`，但注意。
- **pre-commit 钩子拦敏感词**（雇主名等）→ 写实测证据时文件名脱敏。
- **`sliceBetween(…, ")")` 会在参数列表中间截断**（第一个 `)` 常落在
  `Modifier.fillMaxWidth()` 里）→ 用有界窗口或更靠后的锚点。
- **CI 是 tag 驱动的**：`-test.N` tag → 自动 prerelease；正式 tag → 停 draft 等
  人工 Publish。`workflow_dispatch` **不注入版本号**，出的包版本会回落，
  **不能用于自动更新测试**。
