# MOB-52 OPPO Reno8 后台备份不触发且点开 App 闪退（L0）

> ⬜ 状态：未开工 · 当前节点：等崩溃证据（诊断导出/logcat 栈）；下一步：证据到手定位根因，修复走 RED→GREEN · 协同分支：`main`
> 级别：**L0**（闪退=可用性红线；且后台授权失效意味着新照片静默不备份） · 阻塞：等验收人提供崩溃证据

## 问题

2026-09-06 真机（OPPO Reno8，ColorOS，已授予后台运行）：
1. 相册新增图片后 **1 分钟内没有触发上传**（MediaWatch/进程唤醒路径在
   ColorOS 上疑似失效，同族先例：DOG-03 三星 20s 冻进程、MOB-44 鸿蒙退后台停）；
2. 随后**点开 App 直接闪退**——崩溃点未知（启动路径？账本反序列化？
   runtime 初始化？）。

两件事可能同源（后台期间状态写坏 → 冷启动读到坏状态崩），也可能独立。

## 期望行为

- App 冷启动任何情况下不崩溃（账本/存储异常必须降级为可见错误态，不 FATAL）。
- 新增媒体在有后台运行授权的国产 ROM 上，分钟级被发现并上传。

## 验收标准

- [ ] 取证（本卡开工前置，验收人提供）：复现闪退 → 桌面端「导出诊断日志」
      zip（含 crash 信息）或 adb logcat 崩溃栈（`F DEBUG` + `AndroidRuntime`
      段落），附回卡内。
- [ ] 根因定性写回卡：崩在哪个类/函数、为什么（不许「加了 try-catch 就好」
      式无根因修复——历史教训：启发式修复当天返工）。
- [ ] RED：以真实崩溃栈构造复现用例（JVM 可测层），修复后转绿。
- [ ] 后台失效项：ColorOS 上从「添加照片」到「队列出现新 item」≤2 分钟
      （唤醒链路取证：MediaWatch 是否收到回调、Worker 是否被 ROM 杀）。
- [ ] Android JVM 全量绿（报计数）+ debug APK + `just ci`。

## 范围

- 待证据定性后再列（防「按猜测圈范围」）。默认候选：App 启动路径
  （MainActivity/runtime 初始化）、flow-state 反序列化、国产 ROM 唤醒
  适配层（与 DOG-03/MOB-44 同族的适配矩阵）。
- 不准动：账本协议语义、桌面端。

## 阻塞与依赖

前置：**验收人的崩溃日志**。拿不到日志本卡不编码（不许凭描述猜崩点）。

---

## 实施记录

- 2026-09-12：OPPO 真机 v0.5.1 覆盖安装复测，同族症状全部再现（走查记录
  `docs/evidence/2026-09-12-oppo-051-dogfood.md`）：①后台截图等约 1 分钟
  零同步、重开 App 立即补捞；②点进 App 一次「卡崩溃」（配对成功后瞬间，
  图片随后仍传输成功、绿灯）；③存储端「确认断开连接」明显卡顿后才成功。
  崩溃栈仍未到手——本卡前置不变：拿不到诊断导出/logcat 不编码。
  注意观察 ② 与 09-06 那次「点开闪退」可能同点也可能不同点，禁止凭描述
  合并根因。

## 备注

- 三星机上未观察到闪退，暂定性为 ColorOS 相关或数据态相关，等证据。
- 「点开闪退」若与 flow-state JSON 损坏有关，`DiscoveryLedgerStore.load`
  的 catch-回退默认空快照路径本应兜住（空快照导致 UI 装死是 UI-10 的事，
  不导致崩溃）——这条线索指向崩溃可能在 ledger 之外的启动路径。
